import React from 'react';
import { StyleSheet, Text, View } from 'react-native';

const AddressScren = () => {
    return (
        <View>
            <Text>AddressScren</Text>
        </View>
    );
};

export default AddressScren;

const styles = StyleSheet.create({});
